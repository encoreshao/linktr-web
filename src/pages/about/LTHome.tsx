import React from 'react';
import { withRouter } from 'react-router';
import LTHomeContent from './LTHomeContent'

const Home = () => {
  return (
    <LTHomeContent />
  );
}

export default withRouter(Home);
